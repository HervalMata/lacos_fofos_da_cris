import {IsInvalidDirective} from './is-invalid.directive';

describe('IsInvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new IsInvalidDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
